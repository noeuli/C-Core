using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Lab16Test
{
    [TestClass]     // 이게 attribute란다.WCF에서 이걸 젤 많이 사용한단다. 이름은 indexer. [] 안에 무언가를 쓴거.
    // .net reserved keyword만 [] 안에 쓸 수 있단다.
    // 이걸 배우는게 WCF 란다. 이걸 사용하는 문법 등.
    public class UnitTest1
    {
        [TestMethod]
        public void TestMethod1()
        {
        }
    }
}
